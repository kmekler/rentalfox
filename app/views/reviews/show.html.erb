<h1><%= @review.landlord %></h1>

<p>
	Landlord:<%= @review.landlord %>
	Building:<%= @review.building %>
	Unit:<%= @review.unit %>
	Area:<%= @review.area %>
</p>

<%= link_to "Delete this review!", :review, method: :delete, data: {confirm: 'Are you sure you want to delete this review?'} %>

<%= link_to "Back to the list!", reviews_path %>